name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - name: clone into repo
        uses: actions/checkout@v2

      - name: Test
        run: |
          cd src/main/webapp
          test -f index.html
          test -f dotenv.js
